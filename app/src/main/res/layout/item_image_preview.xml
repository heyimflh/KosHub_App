<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="120dp"
    android:layout_height="90dp"
    android:layout_marginEnd="8dp">

    <ImageView
        android:id="@+id/imgPreview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:background="@drawable/bg_mgmt_card"
        android:contentDescription="Preview gambar kos" />

    <TextView
        android:id="@+id/tvCoverBadge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="4dp"
        android:background="@drawable/bg_badge_sisa"
        android:paddingHorizontal="6dp"
        android:paddingVertical="2dp"
        android:text="Cover"
        android:textColor="@color/white"
        android:textSize="11sp"
        android:textStyle="bold"
        android:visibility="gone" />

    <ImageButton
        android:id="@+id/btnRemoveImage"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="top|end"
        android:layout_margin="4dp"
        android:background="@drawable/bg_circle_surface"
        android:src="@drawable/ic_close"
        android:padding="4dp"
        android:scaleType="centerInside"
        android:contentDescription="Hapus gambar"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        app:tint="@color/md_error" />

</FrameLayout>